extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div


      h1 Hello,Projects demonstration Portfolio


      p.lead.text-light
        | This portfolio side is a show case to the potential employers that the projects I have make on half way through the Full Stack JavaScript journey,also it's going to 
        | demonstrate my skills of web development,I list five projects below to show my awesome works.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3



      each data in datas
        .cell 
          a(href=`/project/${data.id}` target="_blank")
            img.thumbnail(src=`${data.image_urls[0]}`)
            h5(class="projectTitle") #{data.project_name}